MEMORY {
    ROM: start = $0, size = 256, fill = yes;
}

SEGMENTS {
    HEADER:   load = ROM, type = ro, start = $0;
    CODE: load = ROM, type = ro, start = $10;
    DISPATCH: load = ROM, type = ro, start = $fe;
}
